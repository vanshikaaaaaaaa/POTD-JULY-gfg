
    int setBits(int N) {
        // Write Your Code here
        
          int count=0;
        while(N!=0)
        {
            int bit = N&1;
            if(bit == 1)
                count++;
            N = N >> 1;
        }
        return count;
    }
